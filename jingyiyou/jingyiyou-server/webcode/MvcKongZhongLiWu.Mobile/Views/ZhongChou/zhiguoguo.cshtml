
@{
    ViewBag.Title = "智裹裹众筹中...";
}

<link href="~/css/sharewx.css" rel="stylesheet" />
<link href="~/css/style.css" rel="stylesheet" />
<style type="text/css">
    .cshead {
        height: 200px;
    }

        .cshead div {
            float: left;
        }

    .tb-prop li {
        float: left;
        position: relative;
        margin: 0 5px 10px 0;
        vertical-align: middle;
        line-height: 20px;
        background: #FFF;
    }

    .tb-prop .tb-selected a, .tb-prop a:hover {
        padding: 2px 5px;
        color: #F40;
        border: 2px solid #F40;
        background: #FFF;
    }

    .tb-prop li a {
        display: inline-block;
        white-space: nowrap;
        text-decoration: none;
        padding: 3px 6px;
        min-width: 10px;
        _width: 10px;
        text-align: center;
        border: 1px solid #DCDCDC;
        background: #FFF;
        -moz-transition-property: border-color,background;
        -o-transition-property: border-color,background;
        -webkit-transition-property: border-color,background;
        transition-property: border-color,background;
        -moz-transition-duration: .2s;
        -o-transition-duration: .2s;
        -webkit-transition-duration: .2s;
        transition-duration: .2s;
    }

    .tb-chicun {
        display: inline-block;
        width: 100%;
    }

        .tb-chicun dt, .tb-chicun li {
            float: left;
            margin-left: 10px;
        }

    .selectpanel {
        position: fixed;
        bottom: 0;
        min-height: 6em;
        max-width: 640px;
        width: 100%;
        background: #fff;
        z-index: 20001;
        display: none;
    }

    .mcover {
        position: fixed;
        top: 0;
        max-width: 640px;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.7);
        z-index: 20000;
        display: none;
    }

    .btn-close {
        position: absolute !important;
        background-image: url(/images/close2.png);
        /*width:1em;
        height:1em;*/
    }
    /*.goods-info .tab-info img{

    }*/
    .hiddencss {
        display: none !important;
    }

    .guanzhu {
        position: relative;
    }
</style>
<script type="text/javascript">
    var fadetime = 300;
    $(document).ready(function () {
        @*$('.buy').click(function () {
            $('#buyform').submit();
        });*@
        setInterval("leftTimer(2017,6,10,23,59,59)", 1000);
        $('.guanzhu').show();
        //$('.con .ect-bg').hide();
        $('.btn-close').click(function () {
            $('.selectpanel').slideUp(fadetime);
            $('.mcover').fadeOut(fadetime);
            $('.btnpanel').show();
        });
        $('.mcover').click(function () {
            $('.btn-close').click();
        });

        $('.panel-content').height($(window).height());
        $('.btn-close').click(function () {
            $('.panel-info').addClass('hiddencss');
        });
        $('.help').click(function () {
            $('.panel-info').removeClass('hiddencss');
        });
        //$('.btnpanel').poshytip('showDelayed', 100);

        //$('.btnpanel').poshytip({content:"222"});
    });
    function changePrice(type) {
        if (type == 'n') {
            $('.form-num').val(parseInt($('.form-num').val()) + 1);
        } else {
            if (parseInt($('.form-num').val()) <= 1) {
                $('.form-num').val(1);
            } else {
                $('.form-num').val(parseInt($('.form-num').val()) - 1);
            }
        }
    }
    function fnshowsurepanel(isme) {
        $('#buyform').submit();
        //if (isme==0) {
        //    $('.panl-notforme').show();
        //    $('.panl-forme').hide();
        //} else {
        //    $('.panl-forme').show();
        //    $('.panl-notforme').hide();
        //}
        //$('.mcover').fadeIn(fadetime);
        //$('.selectpanel').slideDown(fadetime);
        //$('.btnpanel').hide();
    }
    function cl_forme(isme) {
        var property = "";
        //$('.tb-prop-true li').each(function () {
        //    if ($(this).hasClass('tb-selected')) {
        //        property += $(this).data('value') + ',';
        //    }
        //});
        var result = true;
        $('.tb-prop-true').each(function () {
            var selval = "";
            $(this).find('li').each(function () {
                if ($(this).hasClass('tb-selected')) {
                    selval = $(this).data('value');
                }
            });
            if (selval=='') {
                alert('请选择' + $(this).data('property')+"!");
                result = false;
                return false;
            }
            property += selval + ',';
        });
        if (result == false) { return };
        $('#property').val(property);
        $('#forme').val(isme);
        $('#buyform').submit();
    }
    function cl_tag(ulid,pname) {


        $('.ul_' + ulid + ' li').each(function () {

            if ($(this).data('value') == pname) {
                $(this).addClass('tb-selected');
            } else {
                $(this).removeClass('tb-selected');
            }
        });
    }


function leftTimer(year,month,day,hour,minute,second){
 var leftTime = (new Date(year,month-1,day,hour,minute,second)) - (new Date()); //计算剩余的毫秒数
 var days = parseInt(leftTime / 1000 / 60 / 60 / 24 , 10); //计算剩余的天数
 var hours = parseInt(leftTime / 1000 / 60 / 60 % 24 , 10); //计算剩余的小时
 var minutes = parseInt(leftTime / 1000 / 60 % 60, 10);//计算剩余的分钟
 var seconds = parseInt(leftTime / 1000 % 60, 10);//计算剩余的秒数
 days = checkTime(days);
 hours = checkTime(hours);
 minutes = checkTime(minutes);
 seconds = checkTime(seconds);
 
 document.getElementById("timer").innerHTML ="剩余时间："+ days+"天" + hours+"小时" + minutes+"分"+seconds+"秒";
}
function checkTime(i){ //将0-9的数字前面加上0，例1变为01
 if(i<10)
 {
  i = "0" + i;
 }
 return i;
}

</script>


<div class="goods-info ect-padding-tb">
    <section class="ect-margin-tb ect-margin-lr">
        <h4 >智裹裹众筹</h4><br />
        <h5 id="timer"></h5>
        <h5  id="cyrs">参与人数：@ViewBag.rs / 100</h5>
        <h5 id="cyrs">众筹进度：@ViewBag.jd%</h5>
    </section>
  
    @using (Html.BeginForm("DoBuy", "zhongchou", FormMethod.Get, new { id = "buyform" }))
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true)
        
    }

    <section class="user-tab ect-border-bottom0">
        <div id="is-nav-tabs" style="height:3.15em; display:none;"></div>

        <ul class="nav nav-tabs text-center">
            <li id="fororther" class="col-xs-4 active"><a href="#one" role="tab" data-toggle="tab">详细说明</a></li>
        </ul>

        <div class="tab-content">
            <div class="tab-pane tab-info active" id="one">
                <img src="~/images/zgg/intro_01.gif"/>
                <img src="~/images/zgg/intro_02.gif" />
                <img src="~/images/zgg/intro_03.gif" />
                <img src="~/images/zgg/intro_04.gif" />
                <img src="~/images/zgg/intro_05.gif" />
                <img src="~/images/zgg/intro_06.gif" />
                <img src="~/images/zgg/intro_07.jpg" />
                <img src="~/images/zgg/intro_08.jpg" />
                
            </div>
        </div>
        <div class="bottomhoder"></div>
    </section>


</div>
<footer class="logo"></footer>
<div class="btnpanel">
  
        @*<button type="button" class="btn btn-info ect-colorf button_only btn-red" onclick="fnshowsurepanel(0)">参与众筹</button>*@
    <button type="button" class="btn btn-info ect-colorf btnshare" onclick="fnshowsurepanel(0)">299元 参与众筹</button>
    <button class="btn btn-info ect-colorf btnshowroute" type="button" onclick="window.location='/zhongchou/feedback'">留  言</button>

</div>
<div class="mcover">
</div>

